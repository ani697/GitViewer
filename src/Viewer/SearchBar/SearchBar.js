import React from 'react';
import './SearchBar.css';

const SearchBar = () => {
  return (
    <div className="SearchBar">
        <input type="text" placeholder="Search user" id="TextX"/>
    </div>
  );
}

export default SearchBar;
